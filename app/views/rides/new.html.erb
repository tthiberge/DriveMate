<%= simple_form_for @ride do |f| %>
  <%= f.input :car_type %>
  <%= f.input :owner_presence, as: :select, collection: [['Yes',true], ['No',false]], required: true %>
  <%= f.input :seats_available %>
  <%= f.input :description %>

  <%= f.submit %>

<% end %>
